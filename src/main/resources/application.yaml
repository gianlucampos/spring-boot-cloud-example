server:
  port: 8081

spring:
  application:
    name: kafka-migration

  config:
    import: configserver:http://localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: health,refresh
